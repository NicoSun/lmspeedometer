# lmspeedometer
A simple Python tool to benchmark LLMs with LM Studio


## Usage
create a new Python venv with requirements.txt
activate it and run main.py

### Note
only tested on Fedora with Python 3.13
